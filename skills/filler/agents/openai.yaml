interface:
  display_name: "Filler"
  short_description: "Prepare platform copy and fill social drafts with preflight questions, diagnostics, and safe publish boundaries."
  default_prompt: "Use $filler to generate/select copy, run preflight, fill the platform draft, diagnose failures, and stop before final public publish."

policy:
  allow_implicit_invocation: true
